matrix:
  include:
    - os: osx
      language: swift
      osx_image: xcode8.3
script:
  - pod repo add la https://github.com/mitsuse/pods-la.git
  - pod lib lint --allow-warnings --sources='la' --verbose
  - swift test
